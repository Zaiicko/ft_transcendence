// No blur/zoom equivalent exists for a 360° panorama we don't control
// pixel-level — it's either fully explorable or not. Round mechanic is
// therefore Race-only: the panorama is explorable by everyone from round
// start, anyone can guess anytime, first correct guess wins, a shared round
// timer ends it unresolved if nobody finds it. See screenshot-guess's TURNS
// mode for the tour-based alternative this deliberately doesn't have.
//
// No difficulty tiers either — the curated pool is small and hand-verified
// as a whole rather than ranked, so splitting it into easy/normal/hard would
// just thin out an already-small pool for no real benefit.
